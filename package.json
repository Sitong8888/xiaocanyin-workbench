{
  "name": "xiaocanyin-workbench",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "小餐饮爆品与赛道筛选工作台 — 纯静态站点 (GitHub Pages) + Cloudflare Worker 实时数据代理",
  "scripts": {
    "test": "node --test \"tests/**/*.test.js\"",
    "test:watch": "node --test --watch \"tests/**/*.test.js\"",
    "lint": "eslint js cloudflare/worker.js vercel/api tests",
    "lint:fix": "eslint --fix js cloudflare/worker.js vercel/api tests",
    "check": "node --check js/config.js && node --check js/data.js && node --check js/core.js && node --check js/live.js && node --check js/render.js && node --check js/insight.js && node --check js/interactions.js && node --check js/app.js",
    "verify": "npm run check && npm run lint && npm test",
    "deploy:worker": "wrangler deploy --config cloudflare/wrangler.toml",
    "prepare": "husky"
  },
  "devDependencies": {
    "eslint": "^9.14.0",
    "husky": "^9.1.7",
    "jsdom": "^25.0.1"
  },
  "engines": {
    "node": ">=20"
  }
}
